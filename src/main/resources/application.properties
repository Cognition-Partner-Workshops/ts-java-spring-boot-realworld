spring.datasource.url=jdbc:sqlite:dev.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.username=
spring.datasource.password=
spring.jackson.deserialization.UNWRAP_ROOT_VALUE=true

image.default=https://static.productionready.io/images/smiley-cyrus.jpg

# JWT Configuration
# SECURITY: The JWT secret MUST be provided via environment variable in production.
# The default value below is ONLY for local development and MUST NOT be used in production.
# Set the JWT_SECRET environment variable to override this value.
jwt.secret=${JWT_SECRET:dev-only-secret-do-not-use-in-production-change-me-immediately}
jwt.sessionTime=${JWT_SESSION_TIME:86400}

mybatis.configuration.cache-enabled=true
mybatis.configuration.default-statement-timeout=3000
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.use-generated-keys=true
mybatis.type-handlers-package=io.spring.infrastructure.mybatis
mybatis.mapper-locations=mapper/*.xml

logging.level.io.spring.infrastructure.mybatis.readservice.ArticleReadService=DEBUG
logging.level.io.spring.infrastructure.mybatis.mapper=DEBUG
